#############################
# Variables de entorno (ejemplo)
#############################

# Desarrollo rápido (SQLite)
# Si no defines DATABASE_URL, el backend usa SQLite por defecto: sqlite:///./dev.db
# Puedes activarlo explícitamente si quieres:
## SQLite local
# DATABASE_URL=sqlite:///./dev.db
# DATABASE_URL=sqlite:///./dev2.db

## SQLite Cloud (ejemplo)
# DATABASE_URL=sqlitecloud://<host>:<port>/<db>?apikey=<API_KEY>
# Ejemplo real:
# DATABASE_URL=sqlitecloud://cevgmptzvk.g5.sqlite.cloud:8860/HDBD?apikey=YOUR_API_KEY


# Desarrollo local con SQL Server (MSSQL)
# Opción A: LocalDB con autenticación integrada (Windows). Requiere pyodbc y ODBC Driver 17/18.
# Usa URL-encoding para evitar problemas con ( ) y \\ en la cadena.
DATABASE_URL=mssql+pyodbc:///?odbc_connect=DRIVER%3D%7BODBC+Driver+18+for+SQL+Server%7D%3BSERVER%3D(localdb)\\MSSQLLocalDB%3BDATABASE%3DHDBD%3BTrusted_Connection%3DYes%3BTrustServerCertificate%3DYes%3B

# Opción B: Instancia TCP local con usuario/contraseña (p. ej. sa)
# DATABASE_URL=mssql+pyodbc://sa:<password>@localhost:1433/HDBD?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

# Producción (Postgres)
# Alternativa psycopg3 (recomendada):
# DATABASE_URL=postgresql+psycopg://<user>:<password>@<host>:5432/HDBD
# Alternativa psycopg2:
# DATABASE_URL=postgresql+psycopg2://<user>:<password>@<host>:5432/HDBD

# Producción (MSSQL en cloud/servidor)
# DATABASE_URL=mssql+pyodbc://<user>:<password>@<server-host>:<port>/HDBD?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

# Clave secreta para JWT (usa una cadena larga y aleatoria)
SECRET_KEY=replace_this_with_a_long_random_string

# Duración del token en minutos
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Orígenes permitidos para CORS (frontend/host)
CORS_ORIGINS=http://localhost:5173,https://your-frontend.example

# Modo desarrollo: deshabilitar autorización (solo pruebas locales)
# Si se establece a "true", las rutas protegidas aceptan llamadas sin token
# y asumen un usuario simulado (id=1, role=admin) si existe.
# NO activar en producción.
AUTH_DISABLE=true